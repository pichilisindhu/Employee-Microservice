# App details
spring.application.name=project
server.port=8090

# PostgreSQL DB connection
spring.datasource.url=jdbc:postgresql://hrmdb.c3ykgskuuzr8.ap-south-1.rds.amazonaws.com:5432/postgres
spring.jpa.properties.hibernate.default_schema=employee
spring.datasource.username=postgres
spring.datasource.password=HrmAnasolDb
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Multipart upload config
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB

project.image=C:/Users/sindhu/OneDrive/Desktop/Employee_microservice-main/Employee_microservice-main/images/

# If using security keys (can ignore for now if not needed)
 #spring.security.oauth2.resourceserver.jwt.public-key-location=classpath:public.pem
